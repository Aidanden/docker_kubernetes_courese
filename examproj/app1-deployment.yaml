apiVersion: apps/v1
kind: Deployment

metadata:
  name: app1
  namespace: rahmaexam

spec:
  replicas: 1

  selector:
    matchLabels:
      app: app1

  template:
    metadata:
      labels:
        app: app1

    spec:
      containers:
        - name: nginx
          image: nginx:latest

          ports:
            - containerPort: 80

          volumeMounts:
            - name: app1-content
              mountPath: /usr/share/nginx/html/index.html
              subPath: index.html

      volumes:
        - name: app1-content
          configMap:
            name: app1-config
