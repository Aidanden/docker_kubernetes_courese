apiVersion: v1
kind: PersistentVolume

metadata:
  name: postgres18-pv

spec:
  capacity:
    storage: 2Gi

  accessModes:
    - ReadWriteOnce

  persistentVolumeReclaimPolicy: Retain

  storageClassName: manual

  hostPath:
    path: /data/postgres18
    type: DirectoryOrCreate
