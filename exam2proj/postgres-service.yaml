apiVersion: v1
kind: Service
metadata:
  name: postgres18-service
  namespace: rahmapostgres18
spec:
  type: NodePort
  selector:
    app: postgres18
  ports:
    - port: 5432
      targetPort: 5432
      nodePort: 30432
